# 專案封存與交接備忘錄 (Project Handover Note)

**專案狀態**：`Concluded / Archived` (專題結束)
**封存版本**：v4.0
**封存原因**：專題時程結束，雖然功能未達 100% 完美，但保留核心架構。
**主要價值**：**模型訓練記憶**（提供未來快速回憶 CNN-LSTM 訓練流程、資料前處理與骨架生成邏輯）。

---

## 1. ⚠️ 已知核心問題 (Critical Issues)

### 伺服器端 vs 本地端 資料流不一致
這是目前專案功能上的主要斷點。
* **本地端 (`display.py`)**：使用 `binary_cnn_lstm_loader` 與 `multi_cnn_lstm_loader`。直接讀取 JSON 與原始影片檔（可獲取正確解析度），測試結果**成功**。
* **伺服器端**：資料來自前端 YOLOv8 即時傳送（多人骨架、人框、物件框、解析度）。
* **狀況**：兩者提取資料方式不同，導致伺服器端接收的 Tensor 特徵可能與模型訓練時不一致，造成預測不穩定或報錯。

---

## 2. 快速喚醒：模型訓練與資料生成 (Training Memory)

**如果你是為了「重新訓練模型」而打開這個專案，請依序查看以下檔案：**

1.  **資料準備 (`Batch_Video_Inference_....ipynb`)**：
    * 這是 YOLOv8 載入器，用來把影片轉成 JSON。
    * **操作 SOP**：
        1. 一路執行所有 Cell。
        2. **倒數第二個 Cell**：執行生成（依據選定資料夾）。
        3. **倒數第一個 Cell**：用來調整參數（Mode/Path），修改後再回去跑生成。
    * 記得檢查 `cutter.py` 是否需要用來精剪素材。
2.  **資料檢查 (`scan.py`)**：
    * 跑訓練前，先用這個確認過採樣 (Oversampling) 與降採樣 (Undersampling) 後的資料分佈是否合理。
3.  **訓練核心 (`notebook_utils.py`)**：
    * 所有 Trainer 的母體。包含 Dataset Class 定義與訓練迴圈。
    * 回憶重點：注意 `Binary` (只看跌倒瞬間) 與 `Multi` (看躺) 的 Label 定義差異。

---

## 3. 模型邏輯演變 (Model Logic History)

*(保留這段很重要，以免你未來忘記為什麼要拆成兩個模型)*

### A. 動作定義修正
1.  **跌倒 (Fall)**：
    * 舊版：包含「落下」+「倒地/躺」。
    * **新版**：為了配合 Multi 模型識別「躺」，現在 Binary 模型**只看「落下的瞬間」**。
2.  **躺 (Lying)**：
    * 後續倒在地上由 Multi 模型識別為「躺」。
    * **邏輯判斷**：程式碼後續用 `if-else` 判斷這個「躺」是「跌倒後的倒地」還是「單純睡覺/休息」。

### B. 訓練策略
* **獨立 Channel**：每個骨架點擁有獨立 Channel，讓 CNN 理解關節關係。
* **高斯熱圖**：骨架點已改用高斯熱圖；**注意物件框仍是中空框**（這是未來優化點）。
* **解析度校正**：訓練時主動取得原影片解析度以穩定正規化（伺服器版則從前端 JSON 讀取）。

---

## 4. Multi 模型訓練困境與實驗紀錄 (Multi-Model Challenges)

這部分記錄了針對「躺 (Lying)」動作識別的嘗試與結果。

### A. 嘗試解決方案與結果
1.  **調整 Loss Function**：
    * **CE (Cross Entropy)**：因「躺」資料少，模型學不會（False Negative）。
    * **Focal Loss**：雖解決不平衡，但導致模型「過度關注」躺，將彎腰等動作誤判為躺（False Positive）。
2.  **嘗試回退舊版模型 (`v3.9multi`)**：
    * **方案**：透過程式碼調整，成功在 v4.0 環境下掛載舊模型（解決了不相容問題）。
    * **結果**：**線上測試依然失敗**。準確度遠低於本地測試。
    * **結論**：證實問題核心**並非模型架構好壞**，而是 **「伺服器端的資料流處理」** 與訓練環境存在數值或特徵上的隱性差異。

### B. 未來建議
若重啟開發，應優先重寫伺服器端的 Data Receiver，使其輸出與 `display.py` 的 loader 完全一致，並考慮將物件框也改為高斯熱圖。

---

## 5. 歷史關鍵改動摘要 (Key Changelog History)
*(方便快速回顧專案演進，免查 Commit)*

### 核心演算法優化
* **單人追蹤 (Single-Person Tracking)**：鎖定只推論被追蹤的人，有效防止多人在場時的誤判。
* **強制線性插值**：廢除可選的卡爾曼濾波，改為強制使用線性插值補點。
* **Motion Clip**：正式將 `motion_clip` 加入函式庫以利參數調整。

### 模型架構與輸入特徵
* **Channel 分離**：將骨架點從原本擠在同一 Channel，改為各點獨立分層。
* **高斯熱圖 (Gaussian Heatmap)**：在繪製 Relation Map 時，每個骨架點改用高斯熱圖標示。

### 訓練策略備忘 (Training Strategy)
* **正規化修正**：修復了正規化函式中的計算錯誤。
* **Loss 採樣原則**：
    * `Loss: Focal` -> **禁用**降採樣與過採樣。
    * `Loss: CE` -> **必須**平衡正負樣本數量。

## 6. 資料集結構與命名規範 (Dataset & Naming Convention)

**重要**：本專案的 Loader 高度依賴檔名關鍵字來自動標註 Label，請務必遵守以下命名規則，否則訓練時會找不到資料。

### A. 動作關鍵字 (Keywords)
請確保資料夾或檔名包含以下確切字串（注意大小寫與後綴）：

1.  **靜止狀態 (Static States)** - 需加上 `still` 後綴：
    * `sitstill` (坐著/靜坐)
    * `liestill` (躺著/靜臥)
    * **定義**：指身體位置相對固定，無大幅度位移的狀態。

2.  **動態/瞬間動作 (Dynamic/Instant Actions)** - 維持原動作名 (不變)：
    * `walk` (走路)
    * `Fall` (跌倒)
    * **定義**：指身體正在移動或發生劇烈變化的過程。

### B. 資料夾對應 (Folder Structure)
訓練程式 (`notebook_utils.py`) 預設讀取路徑配置：
* **有效資料**：請參閱 `Final_Training_Dataset_v4.0` (或自定義名稱) 資料夾。
* **清洗原則**：保留上述四種關鍵字的 JSON 與剪輯影片。若有舊命名（如 `sitting` 或 `walking_still`）請勿混入，以免 Label 錯亂。

### v4.0 最新實驗性功能 (Untested)
* 邏輯：防止跌倒後的「躺」被誤判為睡眠。
* 防護：新增巨大物件誤判防護、手機鏡頭異常防呆提醒。
* 資料：跌倒儲存時，補上前端傳送的地點資訊。